[general]
enabled = yes
pretty = yes
allowed_origins = *

; PoC only: change this password before using anything but a throwaway
; local Asterisk instance. Must match BRIDGE_ARI_USERNAME/BRIDGE_ARI_PASSWORD
; in asterisk_poc/bridge.env.
[asterisk]
type = user
read_only = no
password = asterisk
