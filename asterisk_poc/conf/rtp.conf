[general]
; Small range: only the Asterisk<->softphone leg uses this (the
; Asterisk<->bridge externalMedia leg stays inside the Docker network and
; doesn't need a published port). Keep this in sync with the port range
; published in docker-compose.yml.
rtpstart=10000
rtpend=10020
