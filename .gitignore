__pycache__/
*.pyc
*.pyo
files/
local_files/

# MongoDB → FerretDB migration dumps (keep locally; do not commit)
backups/mongodb/*.archive.gz
backups/mongodb/*.bson
backups/mongodb/*.json
!backups/mongodb/.gitkeep

# Frontend local env (compose secret); may be empty placeholder
voicera_frontend/.env.local

# Root-level compose secrets (e.g. INTERNAL_API_KEY, read by docker compose's
# automatic .env loading)
/.env

# Jambonz self-hosted stack secrets (local dev/PoC only)
.env.jambonz
jambonz/drachtio.conf.xml
ai4bharat_tts_server/compare_marathi_tts_metrics.py
ai4bharat_tts_server/server_grpc.py
ai4bharat_tts_server/tts_pb2_grpc.py
ai4bharat_tts_server/tts_pb2.py
docs/guides/deployment/mongodb-to-ferretdb.md